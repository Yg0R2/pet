jar {
    enabled = false
}

dependencies {
    api project(':service')

    compile 'org.springframework.boot:spring-boot-starter-web'

    compile group: 'javax.ws.rs', name: 'javax.ws.rs-api', version: '+'
}
