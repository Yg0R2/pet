apply from: "$rootDir/gradle/maven-publish.gradle"

dependencies {
    api group: 'org.springframework', name: 'spring-context'

    api group: 'org.springframework.boot', name: 'spring-boot-starter-data-jpa'

    api group: 'org.apache.commons', name: 'commons-lang3', version: '+'
}
